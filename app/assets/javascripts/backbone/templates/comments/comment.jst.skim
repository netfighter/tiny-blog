div class="media"
  div class="media-body"
    h4 class="media-heading"
      small> by
      = @user.first_name + ' ' + @user.last_name
      small< = $.timeago(@created_at)
      .pull-right
        small
          a href="#" data-confirm="Are you sure?" data-method="delete" class="comment-destroy"
            i> class="glyphicon glyphicon-remove"
            | Delete

    = @content
    hr
